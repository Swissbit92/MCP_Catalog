# Testing dependencies for MCP Coordinator
# Pinned to the versions the suite is validated against (audit 2026-07-04, step 4).
# Bump deliberately, not incidentally — floating >= let CI drift from local.

# Core testing framework
pytest==9.1.1
pytest-cov==7.1.0
pytest-asyncio==1.4.0
pytest-mock==3.15.1
pytest-xdist==3.8.0          # parallel test execution

# Code coverage
coverage[toml]==7.14.2

# Test utilities
faker==40.23.0               # generating test data
freezegun==1.5.5             # mocking datetime
httpx==0.28.1                # async HTTP testing

# Lint / format (also run in CI)
ruff==0.15.18
