# ==============================================================================
# GraphRAG Coordinator + Streamlit Personas — .env
# ==============================================================================

# ------------------------------------------------------------------------------
# 🔌 Coordinator (backend) — used by the UI to call /persona/greet and /persona/chat
# ------------------------------------------------------------------------------
COORD_URL=http://127.0.0.1:8000
COORD_PORT=8000

# ------------------------------------------------------------------------------
# 🤖 LLM / Ollama (Coordinator-side settings; UI doesn’t call Ollama directly)
# ------------------------------------------------------------------------------
# Point the Coordinator at your local Ollama
OLLAMA_BASE=http://127.0.0.1:11434

# Default persona model the Coordinator uses for /persona/*
# (UI reads MODEL name only for display; Coordinator does the actual inference)
PERSONA_MODEL=HammerAI/mythomax-l2:latest
PERSONA_TEMPERATURE=0.1

# ------------------------------------------------------------------------------
# 🧬 Personas data (JSON files the UI reads to render cards & bios)
# ------------------------------------------------------------------------------
# Folder containing personas/eeva.json and personas/cindy.json
PERSONA_DIR=personas

# Optional: which persona to preselect on first load (empty = none/locked tabs)
# Use exact display name OR Coordinator label, e.g.:
#   "Eeva — Nerdy Charming"   (UI display)
#   "Eeva (Nerdy Charming)"   (Coordinator label)
# Note: leaving empty enforces “pick a card first” flow.
DEFAULT_PERSONA=

# ------------------------------------------------------------------------------
# 🎨 Visual assets (point to files under ./ui/images or relative to repo root)
# ------------------------------------------------------------------------------
# App brand
APP_LOGO_PATH=ui/images/app_logo.png

# EEVA
EEVA_LOGO=ui/images/eeva_logo.png
EEVA_AVATAR=ui/images/eeva_avatar.png     # or an emoji like 🧠
EEVA_BG=                                   # optional chat background (e.g., ui/images/eeva_bg.jpg)

# CINDY
CINDY_LOGO=ui/images/cindy_logo.png
CINDY_AVATAR=ui/images/cindy_avatar.png   # or an emoji like 🛠️
CINDY_BG=                                  # optional chat background

# User avatar shown in chat bubbles
USER_AVATAR=ui/images/user_avatar.png

# ------------------------------------------------------------------------------
# ✨ UI behavior toggles (safe defaults)
# ------------------------------------------------------------------------------
# When selecting a different persona card, clear the chat history automatically
UI_CLEAR_ON_SWITCH=false

# Enable extra reveal flair on card selection (glow + balloons)
UI_ENABLE_BALLOONS=true
UI_ENABLE_CARD_GLOW=true

# ------------------------------------------------------------------------------
# 🧰 Future wiring (MCP routing by persona) — placeholders for later
# ------------------------------------------------------------------------------
# Comma-separated list of MCP servers the persona is allowed to use (display only)
# Coordinator will enforce routing later; the UI just shows chips in the Bio tab.
EEVA_ALLOWED_MCP=chat,graphrag
CINDY_ALLOWED_MCP=chat,brave

# ------------------------------------------------------------------------------
# 📝 Notes
# - Paths can be relative; keeping assets under ui/images/ keeps things tidy.
# - UI reads: COORD_URL, PERSONA_MODEL (for the model chip label), PERSONA_DIR,
#   APP_LOGO_PATH, *AVATAR/*LOGO/*BG, USER_AVATAR, UI_* toggles, *_ALLOWED_MCP.
# - Coordinator reads: OLLAMA_BASE, PERSONA_MODEL, PERSONA_TEMPERATURE, COORD_PORT.
# - Leave DEFAULT_PERSONA empty to require users to pick a card before Chat/Bio unlock.
# ------------------------------------------------------------------------------
