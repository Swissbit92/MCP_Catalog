# Coordinator base URL for the UI to call.
# Override per-host if needed; Streamlit reads os.environ first.
COORD_URL=http://127.0.0.1:8000

# Coordinator → Persona Router LLM settings
# The Coordinator already reads these (in server.py) with os.getenv(...).
OLLAMA_BASE=http://localhost:11434
PERSONA_MODEL=llama3.1:8b

# Coordinator → Catalog path for spawning MCPs
COORDINATOR_CATALOG=catalog/graph_rag_mcp.yaml

# Optional: defaults used by your MCP containers (often set inside the containers)
# CHROMA_DIR=.chroma
# CHROMA_COLLECTION=whitepapers
# RAG_OUTPUTS_DIR=outputs/run_simple
# OLLAMA_EMBED_MODEL=nomic-embed-text
# GRAPHDB_URL=http://localhost:7200
# GRAPHDB_REPOSITORY=mcp_kg
