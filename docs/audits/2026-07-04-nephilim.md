---
title: "Repo Audit — nephilim — 2026-07-04"
status: active
created: 2026-07-04
last_reviewed_on: 2026-07-04
review_in: 3 months
applies_to: nephilim
---

# Repo Health Audit — nephilim

**Date:** 2026-07-04 · **Lens version:** 1 · **Metrics:** 614 files, 83,014 source lines (Python 57,142 · TSX 15,539 · TS 8,144)

## 1. Score

| | This audit | Last audit | Δ |
|---|---|---|---|
| **Anchor score** (deterministic, trend-grade) | **42/100** | — | — (first run) |
| Lens quality read (coarse, directional) | ~70/100 | — | — (first run) |

> The **anchor score** is computed by `repo_metrics.py` from hard facts and is reproducible — the same commit always yields 42. It is the figure to **trend**. The **lens quality read** (~70) is the audit agents' holistic judgment; treat it as directional.
>
> **Why they disagree (42 vs 70):** the anchor is dominated by the God-file penalty, which is **capped at its maximum 40 points** because 29 source files exceed 500 lines. The lens read is gentler because ~half those 29 are legitimately-large *test/spec files* (`test_seeker_progression_repository.py` 843, `Chat.test.tsx` 798, four `phase8-*.spec.ts` playwright suites) and vendored config (`webpack.config.js` 755), plus `config.py`'s 855 lines are 12 cleanly-separated settings classes (volume, not tangle). The anchor won't forgive any of that — but it isn't lying either: the *production* god functions it's pointing at (`handle_session_chat`, `prompt_builder`, `query_handler_service`, `startup`) are real, concentrated debt. Trend the 42; read the 70 as "good bones, debt concentrated in a few files."

**Anchor breakdown:** start 100 − god_files 40 (capped) − artifacts 0 − gitignore_gaps 2 − large_files 10 − dead_candidates 6 = **42**

**Primary bottleneck:** Feature accretion into a handful of orchestration files — `chat_session_service.py` (949; `handle_session_chat` is one 426-line function doing 8–10 concerns) and `prompt_builder.py` (920) — held together by `startup.py`, a hand-rolled global-singleton DI hub (~20 module globals) that even *repositories* import, forcing acknowledged circular-import workarounds. Same root cause as the flag debt: every phase (ADR-003→006) added one more `if flag:` block instead of a new collaborator.

## 2. Since last audit

*First audit for this repo — no prior to diff against.* This run is the baseline; the next `/crucible:repo-audit` will open with "anchor 42 → X, closed / new / deferred." The findings below are the opening ledger.

## 3. Lens reports

### 🧹 Janitor — dead code & bloat
- **Biggest safe win — `tests/manual/results/` = 38 tracked files, ~11 MB** of timestamped run reports (`comprehensive_report_*.html/json`, Feb–Jun 2026). Committed output, not fixtures. `[high confidence]`
- **Persona PNGs ~40 MB** — 15 files at 2.4–3.3 MB each under `react-ui/public/images/personas/` are the largest byte contributor (repo is 98 MB total). Re-encode/compress or move to asset storage. `[high]`
- **Dead file (confirmed):** `src/shared/persona_assets.py` — zero references anywhere. `archive/prompt_optimization/*.py` (699 + 592 lines) are self-documented superseded backups. `[high]`
- **Unused npm deps:** `react-window`, `react-virtualized-auto-sizer`, `yaml`, `refractor` (0 src hits); verify `react-syntax-highlighter`, `workbox-webpack-plugin`. All Python deps are used. `[high]`
- **Flag graveyard:** `MEMORY_CONTEXT_INJECT` is the standout — default OFF, config docstring records it **failed its acceptance gate twice** with no scheduled retry. `PERSONA_LEAN_PROMPT` / `ROUTING_SEMANTIC_PRIMARY` / `LORE_ONDEMAND_ENABLED` have all graduated to default-ON → their legacy fallback branches are retirable. `AGENTIC_ENABLED` is early-stage, not graveyard. `[high]`
- **Cleared (not dead):** the 3 `alembic/` candidates are migrations run by the alembic CLI — framework magic, not dead.

### 🏛️ Architect — structure & modularity
- **Verdict: in-between** — a genuinely layered app (routes → services → repositories → models, mirrored on the frontend) whose LLM/prompt/memory/persona subsystem never got its own package (13 loose top-level modules), and whose composition root has calcified into an overgrown hub. Eroding toward big-ball-of-mud *at its center only*.
- **`startup.py` (627) is a high-fan-in DI hub** imported by `server.py`, all 5 routes, and — a layering violation — `repositories/wallet_summary_repository.py`. Real circular import: `chat_session_service.py` carries `# lazy: avoid llm_client<->services cycle`. `[high]`
- **`handle_session_chat` (chat_session_service.py:322)** does 8 documented responsibilities in one ~430-line function. `[high]`
- **`prompt_builder.py` (920)** holds two full parallel pipelines (legacy + lean) side by side. `[high]`
- **`config.py` (855)** = 12 `BaseSettings` classes in one file — volume, not coupling; a pure file-split into a `config/` package. `[low]`
- **No shared API contract** — TS `api/types.ts` hand-duplicates `schemas.py` / `persona_schema.py` shapes; no OpenAPI/codegen anywhere → frontend/backend drift risk. `[medium]`
- **Tests** organize by *kind* (backend/integration/evaluation/manual/e2e) — defensible, not chaos — but `react-ui/tests/` has 4 overlapping `phase8-*.spec.ts` + 3 `celestial-order-*.spec.ts` that look like superseded exploratory suites never consolidated. `[medium]`

### 🔬 Clean Code — refactoring
- **`handle_session_chat` (chat_session_service.py:322-748, 426 lines)** — 8 stacked `if get_settings().<flag>` branches interleaved with business logic; fetches `get_seeker_profile()` twice in adjacent flag blocks (441, 449). Extract phases into a typed `ChatTurnState` instead of `deps: dict`. `[high]`
- **`handle_wallet_query` (query_handler_service.py:437)** — 220 lines, 4-level nesting, a hand-rolled state machine on an untyped `step` int in a plain dict. `match step:` + a `WalletCreationFlow` dataclass. `[high]`
- **Security-adjacent duplication/leak:** `routes/wallet.py:88,206` returns `f"...{str(e)}"` in the HTTP body on **financial endpoints** (leaks internal exception text), while `routes/chat.py` correctly uses `type(e).__name__`. `[medium-high]`
- **Duplication:** wallet 3-slot pre-flight check copy-pasted verbatim (`query_handler_service.py:541-566` vs `605-626`); LLM-completion `try/except→503` triplicated in `routes/chat.py` (237, 286, 329). `[high]`
- **Global mutable session state:** `_wallet_flows: dict = {}` (query_handler_service.py:21) — bare module global, breaks under multiple workers. Plus ~20 `startup.py` singleton globals. `[medium]`
- **91 `except Exception`** blocks with inconsistent log-level policy — rank/capability-context failures drop at `logger.debug` (invisible persona-quality degradation). `[medium]`
- **Modern-language wins:** no `match`/`StrEnum`/walrus anywhere; string pseudo-enums (`source_type`, `role`, `proposal_type`) unconstrained. React is clean (hooks-based, no class components bar the legitimate ErrorBoundary).
- **Flag branching** is *additive*, not yet combinatorial (each flag independently gated) — but the trend is one new `if flag:` per phase; the next flag added to `handle_session_chat` should force the extraction.

### ⚙️ DevOps — config & hygiene
- **[HIGH — security] Documented credential leak, rotation unconfirmed.** ADR-002 records a MongoDB Atlas URI was once committed in a tracked `.env` and pushed; commit `d74886ad` notes "rotate the already-leaked MongoDB/Brave/JWT creds" but **no doc confirms rotation completed**. Confirm + record the rotation date. `[high]`
- **[MED — security] Committed default JWT secret** — `docker-compose.yml:72` `JWT_SECRET_KEY:-change-me-in-production-min-32-chars!!`. Not live (no Docker deploy) but would silently activate under `docker-compose up`. Force-require it, no fallback. `[high]`
- **Excellent tracked-file hygiene:** `tracked_artifacts = 0`; `.env`/`.env.docker` untracked (only `.env.example` tracked); no DBs/caches tracked. `[verified]`
- **Env drift:** `.env.example` declares 6 keys absent from `.env` (incl. `JWT_SECRET_KEY`); `.env` has 10 flag keys undocumented in `.env.example`. Safe only because `AUTH_REQUIRED=false`. `[low]`
- **No CI at all** (no `.github/workflows`) despite a local `--cov-fail-under=60` gate. **No `pyproject.toml`/ruff config** though `.ruff_cache/` exists. `requirements.txt` pinned but `requirements-test.txt` floats. No `.nvmrc`/`engines` despite the documented Node-OpenSSL hack. `[medium]`
- **Config-vs-reality drift:** Docker artifacts fully maintained but the app runs via **launchd on a Mac Mini**; `docs/MAC_MIGRATION_CLEANUP.md` still-open items (NVIDIA/Windows framing in Readme + DOCKER_QUICKSTART, `shell=True` workaround). `[medium]`
- **Root clutter:** loose `chats.db` (0 B) + `test_phase2.db` (155 K) at root instead of `data/` (both gitignored). `.venv` was committed in git *history* (clone-size bloat, not in current tree). `[low]`

### 📄 Docs — cms health (`/crucible:cms check`)
- 2 errors: **`SECURITY.md` missing** and **`docs/THREAT_LEVEL.md` missing** (cms's expected skeleton). CLAUDE.md is 237 lines. Otherwise the `docs/` tree is rich and well-organized (ARCHITECTURE, DEVELOPMENT, ROADMAP, LESSONS_LEARNED, `decisions/` ADRs, `lore/`). Adding the two skeleton files clears cms.

## 4. Refactor priority matrix

| # | Task / File | Issue | Effort | Impact | Actionable fix |
|---|---|---|---|---|---|
| 1 | `routes/wallet.py:88,206` | `str(e)` leaked to client on financial endpoints | **Low** | **High** | Swap to `type(e).__name__`; match `routes/chat.py` |
| 2 | Credential rotation (ADR-002) | Leaked MongoDB/Brave/JWT creds, rotation unconfirmed | **Low** | **High** | Verify + document rotation date in CHANGELOG/ADR-002 |
| 3 | `docker-compose.yml:72` | Committed default JWT secret | **Low** | **Med** | Remove fallback; force-require the env var |
| 4 | `tests/manual/results/` (38 files, 11 MB) | Committed run artifacts, not fixtures | **Low** | **Med** | `git rm` + gitignore the dir |
| 5 | Minimal CI | No safety net for any of the above/below | **Med** | **High** | GH Actions: `pytest` + `npm test`/lint; add `pyproject.toml` (ruff), pin test deps, `.nvmrc` |
| 6 | Flag graveyard | `MEMORY_CONTEXT_INJECT` failed 2× & parked; 3 graduated flags keep legacy branches | **Low–Med** | **Med** | Kill/redesign `MEMORY_CONTEXT_INJECT`; retire graduated flags' legacy paths |
| 7 | `handle_session_chat` (chat_session_service.py:322) | 426-line, 8-concern god function; next flag makes it worse | **Med–High** | **High** | Pure extraction into phase functions + typed `ChatTurnState`; fetch seeker profile once |
| — | Dead code sweep | `persona_assets.py`, `archive/*` backups, 4–6 unused npm deps | **Low** | **Med** | Delete module + backups; `npm uninstall` the verified-unused deps |

## 5. Cleanup plan

Sequenced so each step is protected by the one before it. Steps 1–4 touch no product logic.

1. **Close the security loop (no code).** Verify + document rotation of the MongoDB/Brave/JWT creds (matrix #2); decide on the `AUTH_REQUIRED=false` posture.
2. **Two-line security fixes.** `str(e)` → `type(e).__name__` in `routes/wallet.py` (#1); remove the `docker-compose.yml` default JWT secret (#3).
3. **Zero-risk deletions.** `git rm -r tests/manual/results/` + gitignore (#4); delete `src/shared/persona_assets.py` and `archive/prompt_optimization/*.py`; `npm uninstall react-window react-virtualized-auto-sizer yaml refractor` (verify `react-syntax-highlighter`/`workbox` first); remove loose root `.db` files. Full suite after — should be untouched.
4. **Config hygiene + safety net.** Add minimal GH Actions CI (pytest + npm), `pyproject.toml`/ruff config, pin `requirements-test.txt`, add `.nvmrc`/`engines`, regenerate `.env.example`; add `SECURITY.md` + `docs/THREAT_LEVEL.md` to clear cms (#5). Burn down `MAC_MIGRATION_CLEANUP.md`'s stale-doc items.
5. **Flag retirement (#6).** Decide `MEMORY_CONTEXT_INJECT` (kill with a git tag, or commit to the per-persona-framing redesign); retire the graduated flags' legacy branches. Use `/crucible:flag-gate` — its failure-branch date now names exactly this case.
6. **Mechanical de-duplication.** Extract `_call_llm_or_503()` (3× in `routes/chat.py`), the wallet-slot-limit helper (2×), a shared exception→HTTP translator; `StrEnum` for `source_type`/`role`; bump the silent `logger.debug` catches to `warning`. All behaviour-preserving, all CI-covered by now.
7. **Decompose the god functions (#7).** `handle_session_chat` first (extraction, one seam per PR), then `handle_wallet_query` (onboarding service + repository-backed flow state, killing `_wallet_flows`), then split `prompt_builder.py` (legacy/lean) and `config.py` (→ `config/` package).
8. **Last & biggest: dissolve the `startup.py` hub.** Move to a typed `AppState`/FastAPI `Depends`; migrate the 27 importers incrementally, then delete the lazy in-function imports as the `llm_client ↔ services` cycle dissolves. High effort — do it once the CI/test scaffolding from step 4 is in place.

> Act on findings via `/crucible:develop` — this audit changed nothing (read-only by design). Re-run `/crucible:repo-audit` after a cleanup pass to watch the anchor move off 42.

---
*Generated by `/crucible:repo-audit`. Lenses are frozen (version 1); the anchor score is deterministic. Raw per-lens agent output is gitignored under `docs/audits/.raw/` — only this synthesized report is versioned.*
